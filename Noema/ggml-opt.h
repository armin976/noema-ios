#pragma once

// Wrapper header so upstream llama.h can resolve `#include "ggml-opt.h"` when compiled from the app target.

#include "../External/NoemaLLamaServer/Sources/NoemaLLamaServer/upstream/ggml/include/ggml-opt.h"
